#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * *_strspn - check the code
 * @s: pointer
 * @accept: pointer
 * Return: Always 0.
 */
unsigned int _strspn(char *s, char *accept)
{
	unsigned int i;
	unsigned int l = strlen(s);

	for (i = 0; i < l; i++)
	{
		if (s == accept)
	}
	return (l);
}
